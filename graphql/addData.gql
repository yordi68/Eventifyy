mutation addData($input: [random_insert_input!]!){
        insert_random(objects: $input){
                returning{
                        id
                        title
                        desc
                        value
                }
        }
}