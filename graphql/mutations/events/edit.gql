  mutation editEvent($_set: events_set_input = {}, $id:uuid!, ) {
    update_events_by_pk(_set: $_set, pk_columns: {id:$id}){
      id
    }
    delete_event_tags(where:{event_id:{_eq:$id}}){
      affected_rows
    }
  }
